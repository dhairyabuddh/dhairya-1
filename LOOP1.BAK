#include <stdio.h>
#include <conio.h>

void main() {
	int num = 1;
	clrscr();

	while (num <= 10) {
		printf("%d ", num);
		num++; // num += 1; // num = num + 1;

	}

	/*
	++ -> num += 1 -> num = num + 1
	-- -> num -= 1 -> num = num - 1

	+=
	-=
	*=
	/=
	%=
	*/
	getch();
}