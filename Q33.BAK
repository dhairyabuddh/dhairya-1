#include<stdio.h>
#include<conio.h>
void main() {
	int num,i, check = 1;
	clrscr();
	printf("enter number:");
	scanf("%d",&num);

	for (i=2; i <= num - 1; i++){
		if (num % i == 0)
			check = 0;
	}

	if (check == 1)
		printf("prime number ");
	else
		printf("normal number ");
	getch();
}