#include <stdio.h>
#include <conio.h>

void main() {
	int arr[] = {1, 2, 3}; // array -> numbers
	char str[] = {'a', 'b', 'c'};
	char name[20];
	int i, size = sizeof(name);

	clrscr();

	printf("Enter name : ");
//	scanf("%s", &name);
	gets(name); // input
	puts(name); // output

	printf("name = %s \n", name);

	printf("name[0] = %c \n", name[0]);

	for (i = 0; i < size; i++) {
		printf("%c ", name[i]);
	}
	printf("\n--------------\n");
	for (i = 0; name[i] != '\0'; i++) {
		printf("%c ", name[i]);
	}
	getch();

}